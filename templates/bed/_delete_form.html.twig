<form method="post" action="{{ path('app_bed_delete', {'id': bed.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bed.id) }}">
    <button class="btn">Delete</button>
</form>
